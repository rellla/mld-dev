header:
  version: 1

repos:
  meta-virtualization:
    url: https://git.yoctoproject.org/meta-virtualization
    branch: scarthgap

local_conf_header:
  docker: |
    DISTRO_FEATURES += "seccomp virtualization ipv6"
