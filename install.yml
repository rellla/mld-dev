header:
  version: 1
  includes: 
    - base.yml

target:
  - mld-image-netinstall
  #- mld-image-base

local_conf_header:
  install: |
    DISTRO_FEATURES += "opengl"
    PREFERRED_VERSION_nodejs = "14.%"
