header:
  version: 1
  includes: 
    - base.yml

target:
  - mld-image-netinstall

local_conf_header:
  install: |
    DISTRO_FEATURES += "pam"
    DISTRO_FEATURES += "x11"
    DISTRO_FEATURES += "opengl"
    PREFERRED_VERSION_nodejs = "14.%"
